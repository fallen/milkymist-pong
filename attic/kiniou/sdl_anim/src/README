-Milkymist Democompo with SDL support-


* There is 2 way to compile this democompo:

    - the "Milkymist" way
        make
    
    - the "SDL" way (for debugging purposes on a desktop)
        SDL_DEBUG=1 make

!Note : You must clean the previous compilation in order to compile the other way

* You will also need to install the following packages to compile the SDL way:

    - libsdl1.2-dev
    - mesa-common-dev


